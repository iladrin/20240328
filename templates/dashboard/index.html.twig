{% extends "base.html.twig" %}

{% block body %}
    <h1>Dashboard</h1>

    {% if is_granted('ROLE_USER_LIST') %}
        <a href="{{ path('user_list') }}">See users</a>
    {% endif %}
{% endblock %}
